// config.template.js — VERSION PUBLIQUE (committée sur GitHub)
// Copier ce fichier en config.js et remplir les valeurs
// Ne jamais mettre de vraies valeurs dans ce fichier

const CONFIG = {
  SHEET_ID:      "",   // ID du Google Sheet (visible dans l'URL du Sheet)
  API_KEY:       "",   // Clé API Google (Google Cloud Console → APIs → Identifiants)
  WEBHOOK_URL:   "",   // URL Apps Script Web App (Déployer → Gérer → URL)
  SEUIL_RELANCE: 10    // Jours sans réponse avant alerte (modifiable)
};
